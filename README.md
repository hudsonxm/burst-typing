# burst-typing
Low-latency desktop typing test for short bursts, built in Java and JavaFX. Records per-keystroke timing to surface digraph latency, error clustering, and run-to-run variance rather than a single WPM number.
